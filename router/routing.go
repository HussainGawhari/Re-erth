package router

import (
	"github.com/gin-gonic/gin"
)

func Routings(route *gin.Engine) {

	// fmt.Println("Routing")
	// v1 := route.Group("/v1")
	// //client
	// // .Use(middlewares.Auth())
	// client := v1.Group("/client")

	// {
	// 	client.POST("/client", controller.Addclient)
	// 	client.GET("/client", controller.Getclients)
	// 	client.GET("/client/:name", controller.GetClient)
	// 	client.PUT("/client/:id", controller.EditClient)
	// 	client.PUT("/")
	// }

	// // User
	// user := v1.Group("/user")
	// user.POST("/singup", controller.CreateUser)
	// // user.POST("/login", LoginUser)

}
